# Copy this file to .env and fill in the values.
#
# Server
PORT=4000

# AI Provider (Anthropic used as the interim AIProvider implementation ahead of Konturos)
ANTHROPIC_API_KEY=

# Pricing (EUR per Development Unit)
PRICE_PER_DU=300

# Git operations
GIT_CLONE_TIMEOUT_MS=60000
GIT_MAX_REPO_FILES=5000
